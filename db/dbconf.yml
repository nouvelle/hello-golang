# test:
#   driver: postgres
#   open: user=eriko dbname=golang sslmode=disable

development:
  driver: postgres
  open: user=eriko dbname=golang sslmode=disable
# production:
#   driver: postgres
#   open: user=eriko dbname=golang sslmode=verify-full
# customimport:
#     driver: customdriver
#     open: customdriver open
#     import: github.com/custom/driver
#     dialect: mysql

environment_variable_config:
  driver: $DB_DRIVER
  open: $DATABASE_URL
